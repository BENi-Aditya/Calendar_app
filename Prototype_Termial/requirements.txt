Flask==2.3.3
Flask-Cors==3.1.0  # For handling communication between frontend (React Native) and backend
gunicorn==20.1.0   # For running the Flask app in production
requests==2.31.0   # For making API calls if needed
